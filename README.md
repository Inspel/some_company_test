# labforward_test

Hello! Thank you for reviewing my project. It was fun.
As for improvements, in real project we will need an API layer, error handling and tests. I'd also add ability to switch between stream views instead of rendering all of them, some way to export collected data and ability to access previous parts of stream.

```npm i``` for installation
```npm run dev``` to start dev server

I completed task in 6 hours approximately.
